class Suit {
    name: string;
    letter: string;
    symbol: string;
    constructor(name: string, letter: string, symbol: string) {
        this.name = name;
        this.letter = letter;
        this.symbol = symbol;
    }
}

export { Suit };
